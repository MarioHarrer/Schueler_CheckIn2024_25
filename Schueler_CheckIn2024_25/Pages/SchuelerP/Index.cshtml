@page
@model Schueler_CheckIn2024_25.Pages.SchuelerP.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Schueler[0].email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Schueler[0].schluessel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Schueler[0].passwort)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Schueler) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.schluessel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.passwort)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
